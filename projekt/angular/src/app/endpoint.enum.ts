export enum EndPoint {
  discoveries = "api/discovery",
  events = "api/event",
  encryptDiscoveries = "api/discovery?encrypt=yes",
  voteDiscovery = "api/vote",
  voteComment = "api/votecom",
  logout = "api/logout",
}
